# TODO: kmom10

Innehållsgranskning 2026-08-11. Kvarstående punkter, tas en i taget.

---

1. `index.mdx:82` — stavfel "makrnadsför" → "marknadsför".

2. `index.mdx:104` — dubblerat ord "Din onepage skall vara vara responsiv." → "Din onepage skall vara responsiv."

3. `index.mdx:120` — felaktig accent "spelídé" → "spelidé".

4. `index.mdx:124` — feltypat ord: "Inspelningar kan gälla till exempel svårighetsgraden..." ska vara "Inställningar kan gälla till exempel svårighetsgraden..." (avser konfigurationsinställningar, inte inspelningar).

5. `index.mdx:201` — stavfel "vilken pong och nivå" → "vilken poäng och nivå".

6. `index.mdx:236` — `npm run eslint` finns inte som script i studentmallens `package.json` (som har `lint`, inte `eslint`), ska vara `npm run lint`.

7. `index.mdx` "Krav inlämning" — saknar raden "Ditt repo skall passera `npm run check kmom10`." (finns i motsvarande sektion i kmom03/kmom06).

Åtgärdat: punkt 1 — "makrnadsför" → "marknadsför" (2026-08-11).

Åtgärdat: punkt 2 — dubblerat "vara vara" → "vara" (2026-08-11).

Åtgärdat: punkt 3 — "spelídé" → "spelidé" (2026-08-11).

Åtgärdat: punkt 4 — "Inspelningar" → "Inställningar" (2026-08-11).

Åtgärdat: punkt 5 — "pong" → "poäng" (2026-08-11).

Åtgärdat: punkt 6 — `npm run eslint` → `npm run lint` (2026-08-11).

Åtgärdat: punkt 7 — raden "Ditt repo skall passera `npm run check kmom10`." tillagd i "Krav inlämning" (2026-08-11).
